# Theme
theme = "Solarized Dark Higher Contrast (resources)"
# zsh if it's not default
command = zsh

# Fonts
font-size = 16

# Options
adjust-cell-height = 15%
window-save-state = always
keybind = alt+s>r=reload_config
